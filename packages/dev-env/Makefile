

# "hack": "tsc --build tsconfig.build.json && node --enable-source-maps dist/hack/hack.js $@",
# "example.bubble": "tsc --build tsconfig.build.json && node --enable-source-maps dist/example/bubble.js",
# "example.basic": "tsc --build tsconfig.build.json && node --enable-source-maps dist/example/basic.js",
# "example.accounts": "tsc --build tsconfig.build.json && node --enable-source-maps dist/example/accounts.js"
